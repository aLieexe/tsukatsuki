project:
  name: "my-app"
  domain: "example.com"
  port: 8080
  runtime: "go" # nanti bisa tambah "node", "python", dll

server:
  ip: "192.168.1.10"

webserver:
  type: "caddy"
  ssl_email: "example@mail.com"

github_actions:
  mode: "ci" # ci | ci_cd
  branch: "main"
