services:
{{range .Service}}  {{.}}{{end}}
  app:
    image: {{ .ProjectName }}:latest
    container_name: {{ .ProjectName }}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:{{ .AppPort }}/"]
      interval: 10s
      timeout: 3s
      retries: 3

volumes:
{{range .Volumes}}  {{.}}:
{{end}}