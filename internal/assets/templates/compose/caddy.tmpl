caddy:
    image: {{ .DockerImage }}
    restart: unless-stopped
    ports:
    - "80:80"
    - "443:443"
    volumes:
    - ./conf:/etc/caddy
    - caddy_data:/data
    - caddy_config:/config
    depends_on:
      app:
        condition: service_healthy
